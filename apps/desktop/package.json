{
  "name": "@cloud-wa/desktop",
  "version": "0.1.0",
  "description": "Secure desktop client for WA Control Fabric",
  "author": "WA Control Fabric",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "node scripts/clean-dist.mjs && tsc -p tsconfig.json && concurrently -k \"npm run dev -w @cloud-wa/web\" \"wait-on http://127.0.0.1:5273 && cross-env ELECTRON_RENDERER_URL=http://127.0.0.1:5273 electron .\"",
    "build": "node scripts/clean-dist.mjs && tsc -p tsconfig.json && node scripts/copy-renderer.mjs",
    "test": "node scripts/clean-dist.mjs && tsc -p tsconfig.json && node --test tests/*.test.mjs",
    "pack": "npm run build && electron-builder --dir",
    "dist": "npm run build && electron-builder --win nsis"
  },
  "build": {
    "appId": "com.wacontrol.fabric",
    "productName": "WA Control Fabric",
    "electronVersion": "44.1.1",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**",
      "renderer/**",
      "package.json"
    ],
    "asar": true,
    "asarUnpack": [
      "node_modules/patchright*/**",
      "node_modules/fingerprint-*/**",
      "node_modules/header-generator/**",
      "node_modules/generative-bayesian-network/**"
    ],
    "win": {
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "deleteAppDataOnUninstall": false
    }
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "cross-env": "^10.1.0",
    "electron": "44.1.1",
    "electron-builder": "^26.15.3",
    "wait-on": "^9.1.0"
  },
  "dependencies": {
    "fingerprint-generator": "2.1.88",
    "fingerprint-injector": "2.1.88",
    "patchright": "1.62.2"
  }
}
